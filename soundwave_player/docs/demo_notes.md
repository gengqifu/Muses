# Demo 说明（占位）

当前 Demo 聚焦本地播放（Story10 流式暂缓）。使用方式：
- 在示例输入框填入本地文件 URL（如 `file:///tmp/sample.mp3`），点击 Init -> Load -> Play。
- 波形/频谱视图应刷新；前后台切换后继续播放并可重绘。
- 环境变量可覆盖默认源：`--dart-define=SOUNDWAVE_SAMPLE_URL=file:///path/to/audio.mp3`。

截图占位：待 UI 样式稳定后补充。
